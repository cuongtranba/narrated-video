# Local `make build` output. Anchored at the root on purpose — the committed
# binaries under skills/narrated-video/bin/ are the whole point of the repo.
/bin/

node_modules/
.DS_Store
*.tar.gz

# A project scaffolded for a quick try inside the repo.
/demo/

# Git worktrees kept beside the checkout. They hold a second copy of every doc
# in the repo, which the docs drift test would otherwise walk and judge against
# the wrong branch's command list.
/.worktrees/
