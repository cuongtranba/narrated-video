// GENERATED by `nv sync` — do not edit.
//
// The running order and nothing else. Length lives in ./timeline, so adding a
// scene never touches a frame count.

import type { SceneComponent } from "../scenes/types"
import { Scene as Title } from "../scenes/Title"
import { Scene as Outro } from "../scenes/Outro"

export const SCENE_ORDER = ["Title", "Outro"] as const
export type SceneId = (typeof SCENE_ORDER)[number]

export const SCENE_COMPONENTS: Readonly<Record<SceneId, SceneComponent>> = { Title, Outro }
