// GENERATED by `nv sync` — do not edit.
//
// Imports nothing, deliberately: this is the one module a test or a script can
// load with no bundler, no JSX runtime and no font side effects.

/** Where a scene's length came from. Anything but `measured` is a draft. */
export type Source = "measured" | "estimated" | "literal"

export interface SceneTiming {
  readonly id: string
  readonly leadFrames: number
  readonly narrationFrames: number
  readonly tailFrames: number
  readonly durationInFrames: number
  readonly source: Source
  readonly hasAudio: boolean
}

export interface LocaleTimeline {
  readonly locale: string
  readonly fps: number
  readonly transitionFrames: number
  readonly scenes: readonly SceneTiming[]
  readonly totalFrames: number
  /** False while any narrated scene is estimated or has no audio on disk. */
  readonly complete: boolean
}

export const FPS = 30
export const WIDTH = 1920
export const HEIGHT = 1080

export const DEFAULT_LOCALE = "en"
export const LOCALES = ["en"] as const
export type Locale = (typeof LOCALES)[number]

export const COMPOSITION_ID: Readonly<Record<Locale, string>> = { en: "Explainer" }

export const TIMELINE: Readonly<Record<Locale, LocaleTimeline>> = {
  en: {
    locale: "en",
    fps: 30,
    transitionFrames: 14,
    scenes: [
      {
        id: "Title",
        leadFrames: 14,
        narrationFrames: 67,
        tailFrames: 24,
        durationInFrames: 105,
        source: "estimated",
        hasAudio: false,
      },
      {
        id: "Outro",
        leadFrames: 14,
        narrationFrames: 45,
        tailFrames: 24,
        durationInFrames: 83,
        source: "estimated",
        hasAudio: false,
      },
    ],
    totalFrames: 174,
    complete: false,
  },
}
